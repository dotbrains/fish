# Core Fish shell settings

# Disable the greeting message
set fish_greeting ""

# Abbreviate the current working directory in the prompt
# Shows only the first character of each directory
set -g fish_prompt_pwd_dir_length 1
