# Git aliases

if type -q hub
    alias git "hub"
end

alias acp "git add -A ;and git commit -v ;and git push"

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

# `lazygit` aliases

if type -q lazygit
    alias lg "lazygit"
end
